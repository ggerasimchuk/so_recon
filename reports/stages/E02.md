# E02 — Probabilistic inverse

**Status:** FAIL — `P1_NATIVE_MATRIX_NOT_RUN`

No physical T1/T2/T4 posterior has yet been accepted on the final physics tree. The reduced-v2
design is informative and the separately registered diagnostic quadrature protocol converged
at 257 nodes. This clears the reference stop-gate, but native SMC comparison, noise recovery
and the T1/T2/T4 inference matrix are still incomplete. The pre-registered N32/N64 SMC
matrix completed, but its N64 lower-tail quantile narrowly failed the reference criterion.
A pre-registered N128 diagnostic resolved that discrepancy without changing the sampler,
reference, observations or thresholds, so Task 11.5 is `PASS_WITH_LIMITATIONS`.
The registered 200-seed conditional noise-recovery suite also passes. The remaining hard
stage gate is the T1/T2/T4 native inference matrix and its physical-state diagnostics.
The T2-v2 and T4 truth/control/ambiguity setup gates now pass; this is prerequisite evidence,
not a completed posterior.

## Current native evidence

- E01 dependency: `artifacts/runs/20260915T180437Z-e01-report-df99ba23/E01.json`, accepted
  on clean physics commit `033a9562999a56c442557f8c557981dec07ca69e`; all 13 mandatory
  oil-water checks pass and the stage remains `PASS_WITH_LIMITATIONS` because BO is not run.
- Reduced design: `e02-reduced-corey-v2`, truth seed 701, fixed-noise seed 8000; design check
  `INFORMATIVE`.
- Initial 33-node artifact:
  `artifacts/runs/20260915T133942Z-e02-reduced-reference-9c485ce3/reduced_reference.json`.
- 65-node artifact:
  `artifacts/runs/20260915T141306Z-e02-reduced-reference-cc795699/reduced_reference.json`;
  all 33 parent nodes were verified and reused, and 32 midpoint forwards were added.
- 129-node artifact under the original point-node quantile diagnostic:
  `artifacts/runs/20260915T141509Z-e02-reduced-reference-cc795699/reduced_reference.json`;
  all 65 parent nodes were verified and reused, and 64 midpoint forwards were added.
- Original reference status: `UNRESOLVED_REFERENCE`. The 65→129 mean change is `0.00108289`
  (passes `<0.02`) and the omitted-tail ratio is `6.15894e-09` (passes `<1e-6`), but
  maximum quantile change is `0.109375` (fails `<0.03`) and relative evidence change is
  `0.0343119` (fails `<0.02`). The Q95 moved from `0.875` to `0.984375`.
- Diagnostic protocol: `e02-reduced-reference-diagnostic-v1`. It keeps the same physical
  experiment, truth, observations, prior, likelihood, support and acceptance thresholds,
  but evaluates continuous-z quantiles from the piecewise-linear posterior density CDF.
- Converged 257-node artifact:
  `artifacts/runs/20260915T143601Z-e02-reduced-reference-6c2604d7/reduced_reference.json`.
  Relative to 129 nodes, mean change is `2.34756e-05`, maximum continuous-quantile change
  is `0.00873208`, relative evidence change is `1.76552e-05`, and omitted-tail ratio is
  `5.94762e-09`; all retain and pass the original strict thresholds.
- Accepted numerical reference: mean `0.6958303`, variance `0.02549627`, continuous
  Q05/Q50/Q95 `[0.4795004, 0.6762524, 0.9769650]`, log evidence `-7.7508677`.
  Legacy point-node quantiles `[0.4921875, 0.65625, 0.984375]` remain recorded rather than
  silently reinterpreted.
- Inherited ledger chain: 258 complete forwards (one truth plus 257 unique nodes),
  `345.213 s` summed native wall, `935.198 s` summed CPU and `44,577,887` output bytes.
  The 257-node session reused all 129 parent nodes and added exactly 128 midpoint forwards.
- Refinement code commit: `d650b7c34432cc526f8c2a3ce06de3eeb4a8e9f4`. The native
  run records `git_dirty=true` because the pre-existing unrelated `.idea/so_field.iml`
  edit remained preserved; no source/config/lock diff was present at run time.

## Reduced native SMC checkpoint

- Four separately budgeted runs completed at beta one: N32 seeds 11/12 and N64 seeds
  11/12. Their run artifacts are the immutable parents of
  `artifacts/runs/20260915T153008Z-e02-reduced-smc-comparison-76dbc673/reduced_smc_comparison.json`.
- N32 passes. Pooled mean is `0.6946774` versus reference `0.6958303`; pooled
  Q05/Q50/Q95 are `[0.4346417, 0.6682379, 1.0827559]`, and each deviation is within
  `max(base tolerance, 3 across-start MC SE)`.
- N64 does not pass. Mean and Q50/Q95 pass, but pooled Q05 is `0.3954874` versus
  `0.4795004`: difference `0.0840130` exceeds the unchanged `0.08` tolerance by
  `0.0040130`. Its across-start Q05 MC SE is only `0.0010903`, so it does not widen the
  tolerance.
- All four algorithms completed, used beta histories ending at one, and retained at least
  15 unique ancestors at N32 and 32 at N64. Their mean log-evidence is `-7.76474` (N32)
  and `-7.69902` (N64), versus quadrature `-7.75087`.
- Exact cache hits were reused across paired particle counts. New native forward totals by
  run were 352, 352, 443 and 580; no failed forward was recorded. Every run published a
  checkpoint manifest, ledger, diagnostic summary and posterior bundle.
- Diagnostic N128 seeds 11/12 both completed at beta one with 1,104 and 1,088 new
  forwards, no failures and 64/70 unique ancestors. The immutable paired verdict is
  `artifacts/runs/20260915T161734Z-e02-reduced-smc-comparison-151116e0/reduced_smc_comparison.json`.
  Its pooled mean is `0.6917704`; Q05/Q50/Q95 are
  `[0.5174891, 0.6738848, 0.9640040]`. Differences from quadrature are
  `0.0040599` and `[0.0379887, 0.0023676, 0.0129610]`, all passing unchanged criteria.

## Reduced noise-recovery checkpoint

- Authority:
  `artifacts/runs/20260915T162834Z-e02-noise-recovery-33b4da79/noise_recovery.json`,
  schema `e02-noise-recovery-1`, status `PASS`, 200 independent noise seeds 8000–8199.
- All 200 coarse 33×33 to fine 65×65 Gauss–Hermite refinements converged. Maximum
  changes were `0.000116394` for sigma mean, `0.00106335` for rho mean, `0.00390789`
  for sigma tail quantiles, `0.0291831` for rho tail quantiles and `0.00525163` for
  relative evidence; all are below their pre-registered strict limits.
- Marginal 90% coverage is sigma `0.92` (Wilson-95 `[0.874011,0.950159]`) and rho
  `0.89` (Wilson-95 `[0.839073,0.926228]`); both intervals contain nominal `0.90`.
- Endpoint 0/1 likelihood, missing month 3, reset month 7, dry-prediction refusal and the
  shared-date/two-interval/shared-bias calculation on the exact saved truth states all pass.
- The suite reused one fixed physical prediction bank and started no Julia forward. This is
  conditional noise calibration, not repeated-geology or repeated-physical-world coverage.

## Physical setup checkpoint

- T2-v1 is preserved as `CONTROL_INFEASIBLE`; it did not produce ambiguity evidence.
  Registered T2-v2 changes only the control law to fixed injector/producer BHP 30/5 MPa.
- T2-v2 authority:
  `artifacts/runs/20260915T174621Z-e02-physical-setup-01a5f275/physical_experiment.json`.
  Truth and balances pass. The physical layer-exchange pair gives mean absolute watercut
  gap `0.000161813` and maximum layer-zone So gap `0.159121`, satisfying the unchanged
  ambiguity criteria and yielding `AMBIGUITY_DEMONSTRATED`.
- T4 authority:
  `artifacts/runs/20260915T180505Z-e02-physical-setup-e6c8088e/physical_experiment.json`.
  Two independent one-month closed transient jobs reproduce pressure and So exactly at the
  published precision; initial-state gaps are zero. The non-zero transient remains reported:
  relative pressure `0.000100947`, So `0.0000819911`. Both balance systems pass.
- T4 truth passes, and its remote-state pair gives mean absolute watercut gap `0.000213587`
  and maximum remote-zone So gap `0.171021`, hence `AMBIGUITY_DEMONSTRATED`.
- These setup runs authorize posterior computation but do not satisfy the N32/N64 convergence
  matrix. T1 seeds 141/142 will be republished on the final code identity before their SMC runs.

## Consequence

The thresholds were not widened and the 513/1025/2049 levels were correctly not run after
the first converged level. The reference dependency is open. Native SMC comparison is
`PASS_WITH_LIMITATIONS`: the original N64 marginal failure remains visible, while the
pre-registered doubled-N diagnostic passes. Noise recovery is `PASS`. The T1/T2/T4 native
inference matrix setup is partly complete but its posterior runs are not, so the overall stage remains fail-closed.
Timestamped JSON artifacts and ledgers are the numeric authority.
