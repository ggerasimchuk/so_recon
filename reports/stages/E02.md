# E02 — Probabilistic inverse

**Status:** NOT_RUN

No native E02 evidence has been accepted on the final physics tree. This initial page is a
stage boundary, not a claim that the pure unit suite or an intermediate checkpoint is a
posterior result. Rebuild it with `so-recon e02-report --runs <actual run directories>`.
