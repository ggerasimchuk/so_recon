# E02 — Probabilistic inverse

**Status:** FAIL — `UNRESOLVED_REFERENCE`

No native E02 posterior has been accepted on the final physics tree. The reduced-v2 design
is informative, and the nested P0 reference chain completed one truth plus all 129 unique
quadrature nodes, but the independent reference still does not meet the pre-registered
refinement criteria.

## Current native evidence

- E01 dependency: `artifacts/runs/20260915T133808Z-e01-report-501dac8b/E01.json`, accepted
  on commit `3bb356411f9b3efe42dadd9725ad6f875a8e3c2c` before the evidence-only commit.
- Reduced design: `e02-reduced-corey-v2`, truth seed 701, fixed-noise seed 8000; design check
  `INFORMATIVE`.
- Initial 33-node artifact:
  `artifacts/runs/20260915T133942Z-e02-reduced-reference-9c485ce3/reduced_reference.json`.
- 65-node artifact:
  `artifacts/runs/20260915T141306Z-e02-reduced-reference-cc795699/reduced_reference.json`;
  all 33 parent nodes were verified and reused, and 32 midpoint forwards were added.
- Final 129-node artifact:
  `artifacts/runs/20260915T141509Z-e02-reduced-reference-cc795699/reduced_reference.json`;
  all 65 parent nodes were verified and reused, and 64 midpoint forwards were added.
- Reference status: `UNRESOLVED_REFERENCE`. The 65→129 mean change is `0.00108289`
  (passes `<0.02`) and the omitted-tail ratio is `6.15894e-09` (passes `<1e-6`), but
  maximum quantile change is `0.109375` (fails `<0.03`) and relative evidence change is
  `0.0343119` (fails `<0.02`). The Q95 moved from `0.875` to `0.984375`.
- Inherited ledger chain: 130 complete forwards (one truth plus 129 unique nodes),
  `192.312 s` summed native wall, `488.045 s` summed CPU and `22,461,773` output bytes.
  The final 129-node session itself contains exactly 64 new forwards; it did not recompute
  the 65 inherited nodes.
- Refinement code commit: `d650b7c34432cc526f8c2a3ce06de3eeb4a8e9f4`. The native
  run records `git_dirty=true` because the pre-existing unrelated `.idea/so_field.iml`
  edit remained preserved; no source/config/lock diff was present at run time.

## Consequence

The thresholds were not widened. Native SMC comparison, 200-replicate noise recovery and
the T1/T2/T4 native inference matrix remain `NOT_RUN`; the plan's fail-closed dependency
rule continues to block them. The pre-registered refinement horizon ended at 129 nodes, so
no 257-node run or alternative quadrature was started. Either action requires a separately
recorded extension and run intent. This Markdown file is a readable stage boundary; the
timestamped JSON artifacts and inherited ledgers are the numeric authority.
